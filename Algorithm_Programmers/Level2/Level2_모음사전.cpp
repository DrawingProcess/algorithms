#include <string>
#include <vector>

using namespace std;

int answer = 0;

int solution(string word)
{
    vector<char> alpha = {'A', 'E', 'I', 'O', 'U'};
    do
    {
        
    } while (next_permutation(alpha.begin(), alpha.end()));
    return answer;
}