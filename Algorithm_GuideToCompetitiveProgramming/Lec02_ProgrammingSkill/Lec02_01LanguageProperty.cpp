// #include <bits/stdc++.h>
// 표준 라이브러리가 아니므로 '파일을 따로 추가해 주어야 사용가능'.
// 자주 사용하는 라이브러리들(vector, algorithm, string, 등..)을 컴파일하도록 함으로써 라이브러리들을 '일일이 추가해야하는 번거로움을 없앨 수 있다.'
// 단, 자주 사용하는 라이브러리들을 전부 컴파일함으로써, 사용하지 않거나 불필요한 라이브러리들도 컴파일이 되므로 그만큼 '시간이나 공간이 낭비'된다.
// g++ 컴파일러의 기능이며, g++ -std=c++11 -02 -Wall <cpp파일명>.cpp -o <실행파일명>
// include "stdc++.h"

// + #include <bits/stdc++.h> 사용방법
// C:\Program Files (x86)\Microsoft Visual Studio\2019\Community\VC\Tools\MSVC\14.24.28314\include
// 경로에서 bits 폴더를 생성한다. (해당 경로는 Visual studio가 설치된 기본적인 곳으로 없다면 본인이 찾아야 한다.)
// 그리고 생성한 bits 폴더 안에 다운 받은 stdc++.h 헤더 파일을 넣어주면 된다.
// 이제 #include <bits/stdc++.h> 를 선언하면 사용할 수 있다.

// c++ 형식 입출력
#include <iostream>
using namespace std;

// 입출력을 효율적으로
ios::sync_with_stdio(0);
cin.tie(0);

// 입력 데이터의 양을 알 수 없다면?
while (cin >> x) {

}

// 코드를 간결하게
typedef long long ll;
ll a = 9876543210;